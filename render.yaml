services:
  - type: web
    name: studentera-app
    env: node
    nodeVersion: 16.20.0
    buildCommand: npm install
    startCommand: node simple-server.js
    envVars:
      - key: PORT
        value: 10000
